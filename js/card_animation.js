/* const listItems = document.querySelectorAll('.list_services__item');

listItems.forEach(item => {
  item.addEventListener('mouseover', () => {
    // Aplicar estilos cuando el cursor está sobre el elemento
    item.classList.add("list_services__item--blue")
  });

  item.addEventListener('mouseout', () => {
    // Restablecer los estilos cuando el cursor sale del elemento
    item.classList.remove("list_services__item--blue")
  });
});
 */